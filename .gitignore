# Project-specific configuration
# Each project sets its own base_workspace_path, worktrees_folder, etc.
# Use config.example.yaml as template

# BMAD and Treehouse
_bmad
_bmad-output
.treehouse
CLAUDE.md
.claude

# Treehouse (th) - Tracked artifacts
# These paths are per-workspace and synced via th workflows
# Do not remove - managed by /bmad:th:workflows:treehouse-init
docs/
