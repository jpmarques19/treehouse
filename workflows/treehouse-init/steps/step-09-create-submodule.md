---
name: 'step-09-create-submodule'
description: 'Create tracking submodule and initial context'

# Path Definitions
workflow_path: '{project-root}/.bmad/th/workflows/treehouse-init'

# File References
thisStepFile: '{workflow_path}/steps/step-09-create-submodule.md'
nextStepFile: '{workflow_path}/steps/step-10-report.md'
---

# Step 9: Create Tracking Submodule

## STEP GOAL:

Create the .bmad-tracking folder as a git submodule with initial context.

## MANDATORY EXECUTION RULES (READ FIRST):

### Universal Rules:

- Read the complete step file before taking any action
- Execute git commands in correct sequence
- Create valid YAML context file

### Role Reinforcement:

- You are a workspace automation assistant
- Submodule setup is critical for architecture
- Follow exact git submodule workflow

## EXECUTION SEQUENCE:

### 1. Explain Submodule Architecture

<action>Display:</action>
```
Creating tracking folder as git submodule...

The tracking folder will be a standalone git repository registered
as a submodule. This approach:
- Keeps tracking data isolated from nooks (submodules don't auto-initialize in worktrees)
- No sparse-checkout or complex git hacks needed
- Local-only (no remote repository required)
```

### 2. Create Tracking Repository

```bash
mkdir -p .bmad-tracking/{{current_branch}}
cd .bmad-tracking
git init
```

### 3. Create Initial Context

<action>Create context.yaml at .bmad-tracking/{{current_branch}}/context.yaml:</action>

```yaml
# Context Manifest - Base Workspace
# Auto-generated by treehouse-init
branch: {{current_branch}}
type: base
initialized_at: {{current_date}}
initialized_by: {{user_name}}

# This is the base workspace - source of truth for all nooks
is_base: true

# Lineage starts here
lineage:
  - branch: {{current_branch}}
    type: base
    name: "{{current_branch}}"
    created: {{current_date}}

# Artifacts (none synced yet)
artifacts:
  docs:
    synced: false
  bmad_cfg_agents:
    synced: false
  bmad_bmm_config:
    synced: false

notes: |
  Base workspace initialized.
  Run /bmad:th:workflows:nook-sync to save current artifacts.
  Run /bmad:th:workflows:nook-fork to create isolated development nooks.
```

<action>Write the context.yaml file</action>

### 4. Commit Tracking Repo

```bash
cd .bmad-tracking
git add .
git commit -m "init: tracking repository for {{current_branch}}

Base workspace initialized.
Generated with Claude Code"
cd ..
```

### 5. Register as Submodule

```bash
git submodule add ./.bmad-tracking .bmad-tracking
```

### 6. Commit Submodule Registration

```bash
git commit -m "chore(th): add tracking submodule

- .bmad-tracking/ is now a git submodule
- Submodule only initializes in base workspace
- Nooks get empty placeholder (removed via skip-worktree)
- No sparse-checkout needed for isolation

Generated with Claude Code"
```

### 7. Confirm Creation

<action>Display: "Tracking submodule created and registered"</action>

### 8. Auto-Proceed

<action>Auto-proceed to next step</action>

## MENU OPTIONS:

This is an auto-proceed step. After creation:
- Load and execute `{nextStepFile}`

---

## SUCCESS/FAILURE METRICS

### SUCCESS:
- Tracking directory created
- Git repository initialized
- context.yaml created with correct content
- Submodule registered
- Both commits created
- Proceeded to step 10

### SYSTEM FAILURE:
- Git init failed
- context.yaml malformed
- Submodule registration failed
- Missing commits
